# Save study metadata to CSV

Save study metadata to CSV

## Usage

``` r
saveObjectMetadata(object, path, studyName = NULL)
```

## Arguments

- object:

  Object to save

- path:

  Path to CSV file

- studyName:

  Optional study name override
